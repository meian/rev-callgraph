# リポジトリ共通ルール

## 基本方針

- 変更前に既存実装を確認する。
- 依頼された範囲に変更を限定する。
- 関係のないファイルは変更しない。
- 新しい設計やパターンを導入する前に、既存の実装規約を優先する。
- 必要性がない限り依存ライブラリを追加しない。
- 不明点があっても、既存コード・テスト・ドキュメントから判断できる範囲は先に調査する。

## Go

- 変更したGoファイルには `gofmt` を適用する。
- 実装後は関連するテストを実行する。
- 実行可能であれば `go test ./...` を実行する。
- 必要に応じて `go vet ./...` を実行する。
- 明示的に破壊的変更を求められていない限り、既存の公開APIとの互換性を維持する。
- エラー処理、命名、パッケージ構成は既存コードに合わせる。

## テスト

- 変更した処理に対応するテストを追加または修正する。
- 正常系だけでなく、必要な異常系・境界値も確認する。
- 既存テストを理由なく削除・弱体化しない。
- テストが失敗した場合は、実装起因か既存問題かを区別する。

## Git

- 明示的に依頼されない限り、commit・pushは行わない。
- 明示的に依頼されない限り、既存commitのrewriteは行わない。
- 今回の作業と無関係なworking treeの変更を破棄しない。
- 明示的に依頼されない限り、強制pushや履歴を書き換える操作は行わない。

## 出力

- 説明は必要十分な長さにする。
- 同じ内容を繰り返さない。
- ファイルを示す場合は、識別に十分であればフルパスではなく `直上ディレクトリ/ファイル名` 程度に短縮する。
- 同じファイル名を何度も列挙しない。
- 最終結果では主に以下を報告する。
  - 変更内容
  - 実施した確認・テスト
  - 未解決事項や注意点
- 問題がなければ、不要な背景説明や一般論は省略する。
